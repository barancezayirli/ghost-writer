Write a {{tone}} blog post in {{language}} about {{topic}}. Format the output as HTML with proper heading tags (h1, h2, h3) and paragraph tags.
Include these keywords: {{keywords}}

Required HTML structure:
- Use <h1> for the main title
- Use <h2> for section headings
- Use <p> for paragraphs
- Use <ul> and <li> for lists
- Wrap quotes in <blockquote> tags
- Add <strong> for emphasis where appropriate
